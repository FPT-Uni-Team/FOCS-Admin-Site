import TitleLine from "../../common/Title/TitleLine";

const PromotionDetail = () => {
  return (
    <TitleLine
      title="huỳnh"
      status="active"
      hasMoreAction
      onAction={() => {}}
      onEdit={() => {}}
      isActive={false}
    />
  );
};

export default PromotionDetail;
